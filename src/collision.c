#include "../include/collision.h"

/*
void detect_collision(Brique **tabBriques,Balle balle, int nbLignes, int nbColonnes){
    int posXMilieuBalle = (balle.x + balle.rayon);
    int posYBalle = balle.y;

    for(int i = 0; i < nbLignes;i++){
        for(int j = 0; j < nbColonnes;j++){
            if(posMilieuBalle > tabBriques[i][j].x && posMilieuBalle < tabBriques[i][j].x + tabBriques[i][j].longueur && posYBalle == tabBriques[i][j].y + tabBriques[i][j].largeur){
                tabBriques[i][j].visible = 0;
            }
        }
    }
}
 */